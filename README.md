# Setup

### Installation

```
npm install
cp .env.example .env
```

### Start

Before start the server, Make sure you has been filled `LINE Access Token` and `LINE Secret` in `.env` file.

```
node index.js
``` 